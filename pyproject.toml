[project]
name = "reqiter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "tqdm>=4.60",
]

[project.scripts]
reqiter = "reqiter.main:main"

[dependency-groups]
dev = []
