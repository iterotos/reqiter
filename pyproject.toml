[project]
name = "reqiter"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "exrex>=0.12.0",
    "tqdm>=4.60",
]

[project.scripts]
reqiter = "reqiter.main:main"

[dependency-groups]
dev = []
